export interface NavLink {
  label: string;
  href: string;
  cta?: boolean;
}

export interface SchoolInfo {
  name: string;
  tagline: string;
  description: string;
  email: string;
  phone: string;
  address: string;
}
